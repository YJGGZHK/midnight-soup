import {readFile,mkdir,writeFile} from 'node:fs/promises';import {build} from 'esbuild';
const dir=new URL('../../public/',import.meta.url), read=f=>readFile(new URL(f,dir),'utf8');const app=await read('app.js');const bridge=await readFile(new URL('../web-bridge.js',import.meta.url),'utf8');const out=await build({stdin:{contents:`${app.split('\n')[0]}\n${bridge}\n(async()=>{${app.substring(app.indexOf('\n'))}\n})();`,resolveDir:dir.pathname},bundle:true,write:false,format:'iife',minify:true});let html=await read('index.html');html=html.replace('<link rel="stylesheet" href="/style.css">',`<style>${await read('style.css')}</style>`).replace('<script type="module" src="/app.js"></script>','').replace('class="brand" href="/"','class="brand" href="#"').replace('</body>',`<script>${out.outputFiles[0].text}</script></body>`);await mkdir(new URL('../generated/',import.meta.url),{recursive:true});await writeFile(new URL('../generated/web.js',import.meta.url),`export default ${JSON.stringify(html)};\n`);console.log('Bundled',html.length);
