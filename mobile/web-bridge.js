const pending=new Map();let seq=0;window.MidnightSoup={request(path,body){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});window.ReactNativeWebView.postMessage(JSON.stringify({id,path,body}));setTimeout(()=>{if(pending.delete(id))reject(Error('操作超时，请重试'))},45000)})},receive(x){const p=pending.get(x.id);if(!p)return;pending.delete(x.id);x.error?p.reject(Error(x.error)):p.resolve(x.data)}};window.addEventListener('soup-back',()=>{const d=document.querySelector('dialog[open]');if(d)d.close('no');else window.MidnightSoup.request('exit')});
